require "spec_helper"

describe InvalidViewTool do
  it "has a version number" do
    expect(InvalidViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
